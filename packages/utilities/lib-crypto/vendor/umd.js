(function(root, factory) {
    if (typeof exports === 'object' && typeof exports.nodeName !== 'string') {
        /* global require */
        factory(exports);
    } else {
        root.deip = root.deip || {};
        factory((root.deip.sjcl = {}));
    }
})(this, function(exports) {

    // INSERT

});